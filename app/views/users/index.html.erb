<% provide(:title, 'Index') %>
<h1>ユーザー一覧</h1>
<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<% if @users.any? %>
			<%= render @users %>
		<% end %>
			<%= will_paginate %>
	</div>	
</div>
