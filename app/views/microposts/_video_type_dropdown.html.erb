<div class="dropdown search-dropdown">
    <a href="#" class="dropdown-toggle header-link" data-toggle="dropdown">
      ジャンル <b class="caret"></b>
    </a>
    <!-- 検索のコントローラーとビューを編集 -->
    <ul class="dropdown-menu">
      <li><%= link_to "全ジャンル",  microposts_path(sort_version: params[:sort_version], search_version: params[:search_version],
      		 video_type: "全ジャンル") %></li>	
      <% video_type_array.each do |video_type| %>
              <li><%= link_to video_type,  microposts_path(sort_version: params[:sort_version], search_version: params[:search_version],
           video_type: video_type) %></li>
      <% end %> 
     </ul>
</div>
